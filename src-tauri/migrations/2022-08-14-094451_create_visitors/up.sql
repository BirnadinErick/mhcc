CREATE TABLE visitors
(
    v_id    INTEGER PRIMARY KEY,
    name    TEXT    NOT NULL,
    address TEXT    NOT NULL,
    tpno    TEXT    NOT NULL,
    dob     INTEGER NOT NULL,
    nic     TEXT    NOT NULL
);
